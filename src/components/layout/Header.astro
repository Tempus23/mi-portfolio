---
import ThemeToggle from "@/components/ui/ThemeToggle.astro"
import cvData from "@/data/cv_data.json"
const { basic_info } = cvData;
const navItems = [
  {
    title: "Experience",
    label: "experience",
    url: "/mi-portfolio/#experience",
  },
  {
    title: "Projects",
    label: "projects",
    url: "/mi-portfolio/#projects",
  },
  {
    title: "Skills",
    label: "skills",
    url: "/mi-portfolio/#skills",
  },
  {
    title: "About Me",
    label: "about-me",
    url: "/mi-portfolio/#about-me",
  },
  {
    title: "Contact",
    label: "contact",
    url: `mailto:${basic_info.emails[0]}`,
  },
]
---

<header
  class="fixed top-0 z-10 flex items-center justify-center w-full mx-auto mt-2"
>
  <nav
    class="flex px-3 text-sm font-medium rounded-full text-gray-600 dark:text-gray-200 justify-center items-center"
  >
    {
      navItems.map((link) => (
        <a
          class="relative block px-2 py-2 transition-colors duration-200 hover:text-indigo-600 dark:hover:text-indigo-400"
          aria-label={link.label}
          href={link.url}
        >
          {link.title}
        </a>
      ))
    }
    <ThemeToggle />
  </nav>
</header>

<script>
  document.addEventListener("astro:page-load", () => {
    const sections = document.querySelectorAll("section")
    const navItems = document.querySelectorAll("header nav a")

    const callback = (entries: any[]) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          navItems.forEach((item) => {
            if (item.getAttribute("aria-label") == entry.target.id) {
              item.classList.add("text-indigo-600", "dark:text-indigo-400")
            } else {
              item.classList.remove("text-indigo-600", "dark:text-indigo-400")
            }
          })
        }
      })
    }

    const observer = new IntersectionObserver(callback, {
      root: null,
      rootMargin: "0px",
      threshold: 0.3,
    })

    sections.forEach((section) => {
      observer.observe(section)
    })

    document.onvisibilitychange = () => {
      if (document.visibilityState === "hidden") {
        observer.disconnect()
      } else {
        sections.forEach((section) => {
          observer.observe(section)
        })
      }
    }
  })
</script>

<style>
  nav {
    animation: nav-shadown 1s linear both;
    animation-timeline: scroll();
    animation-range: 0 100px;
  }

  nav {
    @apply dark:bg-gray-900/80 bg-white/70 backdrop-blur-md;
  }

  @keyframes nav-shadown {
    0% {
      @apply dark:bg-gray-900/0 bg-white/0;
    }
    to {
      @apply shadow-lg ring-1 backdrop-blur-xl ring-black/5 dark:ring-white/10;
    }
  }
</style>
